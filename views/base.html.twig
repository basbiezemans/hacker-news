<!DOCTYPE html>
<html lang="en">
<head>
  <title>Hacker News</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  <style>
    /* Remove the navbar's default margin-bottom and rounded borders */ 
    .navbar {
        margin-bottom: 0;
        border-radius: 0;
    }
    
    /* Set height of the grid so .sidenav can be 100% (adjust as needed) */
    .row.content {
        height: 100%;
    }
    
    /* Set 100% height */
    .sidenav {
        padding-top: 20px;
        height: 100%;
    }
    
    .text-left {
        margin-top: 20px;
        margin-bottom: 10px;
    }
    
    .item-rank {
        float: left;
        text-align: right;
        width: 20px;
        margin-right: 5px;
    }
    
    .item-text {
        overflow: hidden;
    }
    
    .toggle {
        cursor: pointer;
    }
    
    div.children {
        padding-left: 20px !important;
    }
    
    body {
        background-color: #ececec;
    }
    
    button {
        margin-left: 5px;
    }
    
    small { color: gray; }
    
    /* On small screens, set height to 'auto' for sidenav and grid */
    @media screen and (max-width: 767px) {
        .sidenav {
            height: auto;
            padding: 15px;
        }
        .row.content { height:auto; } 
    }
  </style>
</head>
<body>

<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="{{ app.url_generator.generate('items') }}">Hacker News</a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li{% if '/new' in url %} class="active"{% endif %}><a href="{{ app.url_generator.generate('items', {'type': 'new'}) }}">New</a></li>
        <li{% if '/best' in url %} class="active"{% endif %}><a href="{{ app.url_generator.generate('items', {'type': 'best'}) }}">Best</a></li>
        <li{% if '/show' in url %} class="active"{% endif %}><a href="{{ app.url_generator.generate('items', {'type': 'show'}) }}">Show</a></li>
        <li{% if '/ask' in url %} class="active"{% endif %}><a href="{{ app.url_generator.generate('items', {'type': 'ask'}) }}">Ask</a></li>
        <li{% if '/jobs' in url %} class="active"{% endif %}><a href="{{ app.url_generator.generate('items', {'type': 'jobs'}) }}">Jobs</a></li>
        <!-- <li><a href="#">Submit</a></li> -->
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="#"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
      </ul>
    </div>
  </div>
</nav>
  
<div class="container-fluid text-center">    
  <div class="row content">
    <div class="col-sm-2 sidenav"></div>
    <div class="col-sm-8 text-left">
    {% block main %}{% endblock %}
    </div>
    <div class="col-sm-2 sidenav"></div>
  </div>
</div>

{% block javascript %}{% endblock %}

</body>
</html>
